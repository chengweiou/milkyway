spring:
  h2:
    console:
      enabled: true
  sql:
    init:
      platform: h2
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:test;MODE=PostgreSQL

site:
  andromeda: http://127.0.0.1:60002/
